# Docker & MySQL Containers

Use official image, mount data volume, set passwords via env or secrets. Use `--character-set-server` and `--collation-server`.

> 📘 Next: [Kubernetes StatefulSet for MySQL](04-kubernetes-statefulset-for-mysql.md)
