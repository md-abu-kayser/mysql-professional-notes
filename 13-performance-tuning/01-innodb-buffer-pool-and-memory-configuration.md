# InnoDB Buffer Pool & Memory Configuration

Most critical tuning parameter. Set to ~70‑80% of RAM on dedicated DB server. `innodb_buffer_pool_size`. Other: `innodb_log_file_size`, `tmp_table_size`.

> 📘 Next: [Query Cache & Optimizer Hints](02-query-cache-and-optimizer-hints.md)
