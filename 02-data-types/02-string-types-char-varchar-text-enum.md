# String Types

- `CHAR(n)` fixed length.
- `VARCHAR(n)` variable length (max 65,535 bytes).
- `TEXT`, `MEDIUMTEXT`, `LONGTEXT`.
- `ENUM` and `SET` (use with care).

> 📘 Next: [Date & Time Types – DATE, DATETIME, TIMESTAMP](03-date-and-time-types-date-datetime-timestamp.md)
