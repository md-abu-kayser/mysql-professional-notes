# UNION, INTERSECT, EXCEPT

`UNION` combines results, `UNION ALL` keeps duplicates. `INTERSECT` and `EXCEPT` supported in MySQL 8.0+.

> 📘 Next: [Common Table Expressions (CTE) & Recursive CTE](06-common-table-expressions-cte-recursive.md)
